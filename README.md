# APL — AI Programming Language

APL is an experimental programming language and execution architecture designed AI-first: programs are represented by explicit, typed, verifiable semantics rather than human-oriented syntax alone.

This repository is being bootstrapped from a minimal reference implementation. The immediate goal is a deterministic canonical IR, verifier, reference interpreter, CLI, and conformance tests.

> Status: pre-alpha / language design in progress.
